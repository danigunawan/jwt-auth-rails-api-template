# Token-based authentication w/ Rails 5 API
- Template for Rails API w/ JWT

## Usage
- `git clone` this repo 
- `bundle`
- run `rake db:create db:migrate` to set up the database
- `rails s` to start development server
